require 'rspec'
require 'player'

RSpec.describe "Player class" do
  it "has a hand"
  it "has a pot"
  it "can discard from hand"
  it "bet from pot"
  it "can fold, see, and raise"
  it "can bluff"
end